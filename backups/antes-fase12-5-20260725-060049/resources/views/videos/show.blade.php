@extends('layouts.app',['title'=>$video->title.' - Luzicity'])
@section('content')<section class="content-band"><p class="eyebrow">{{ $video->category?->name ?: 'Vídeo' }}</p><h1>{{ $video->title }}</h1><p>{{ $video->description }}</p></section><section class="settings-panel">@php($embed=$player->embedUrl($video))@if($embed)<iframe src="{{ $embed }}" title="{{ $video->title }}" allowfullscreen style="width:100%;aspect-ratio:16/9;border:0"></iframe>@else<video controls preload="metadata" poster="{{ $video->thumbnail_path?asset('storage/'.$video->thumbnail_path):'' }}" style="width:100%"><source src="{{ $video->publicUrl() }}">@if($video->subtitle_path)<track kind="subtitles" src="{{ asset('storage/'.$video->subtitle_path) }}" srclang="{{ $video->subtitle_language }}" default>@endif</video>@endif</section>
@endsection
